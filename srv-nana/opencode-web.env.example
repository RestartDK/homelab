DOMAIN=chateauducipieres.com
OPENCODE_SUBDOMAIN=opencode
OPENCODE_HOSTNAME=0.0.0.0
OPENCODE_PORT=4096
# Optional override. Defaults to "$HOME/Projects" for the service user.
# OPENCODE_WORKDIR=/srv/projects
# Optional basic auth. Leave unset if access is already restricted upstream.
# OPENCODE_SERVER_USERNAME=opencode
# OPENCODE_SERVER_PASSWORD=change-me
